# Core
anthropic==0.40.0
python-dotenv==1.0.1
pydantic==2.9.2
requests==2.32.3

# GitHub mining
PyGithub==2.4.0

# Docker-based verifier
docker==7.1.0

# Evaluation
scikit-learn==1.5.2

# Orchestration — mining/verification pipeline (see DEVELOPER.md)
dagster==1.13.8
dagster-webserver==1.13.8

# SFT — install separately on GPU machine:
# pip install torch --index-url https://download.pytorch.org/whl/cu124
# pip install transformers==4.45.2 peft==0.13.0 trl==0.11.4 accelerate==0.34.2 bitsandbytes==0.44.1 datasets==3.0.1
