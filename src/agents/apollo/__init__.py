"""Phase 1 reproduction scaffold: Apollo-style prompt-only monitor evaluated
against deceptron's real-repo trajectories, compared to Apollo's reported
synthetic-trained (STRIDE/Gloom -> ControlArena) numbers. See docs/plan.md."""
